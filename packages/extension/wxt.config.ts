import { defineConfig } from 'wxt';

// See https://wxt.dev/api/config.html
export default defineConfig({
  manifest: {
    name: 'Hotstar Sync',
    description: 'Watch JioHotstar together, in sync — a Teleparty-style watch party.',
    version: '0.1.0',
  },
});
